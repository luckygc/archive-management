## 1. 字段元数据合同

- [x] 1.1 在 `am_archive_field` 增加编辑控件、表格/详情/编辑独立布局字段
- [x] 1.2 扩展后端字段 DTO、请求模型、Mapper 和校验
- [x] 1.3 扩展前端 archive 类型和独立布局配置区
- [x] 1.4 增加公共布局配置表，并按公共布局、字段默认配置合成有效布局
- [x] 1.5 使用拖拽库实现所见即所得布局配置

## 2. 记录详情和编辑接口

- [x] 2.1 新增档案记录详情 DTO，返回固定字段、分类、字段定义和动态字段值
- [x] 2.2 新增 `GET /api/v1/archive-records/{id}` 详情接口
- [x] 2.3 新增 `PATCH /api/v1/archive-records/{id}` 编辑接口
- [x] 2.4 更新记录时维护全文检索投影和操作审计

## 3. 前端详情、编辑和动态控件

- [x] 3.1 抽出动态字段编辑控件组件，支持 textarea、number、date、datetime 和 input
- [x] 3.2 新增档案记录详情页，按详情布局展示字段
- [x] 3.3 新增档案记录编辑页，按编辑布局渲染表单并保存
- [x] 3.4 档案库列表按列宽配置渲染，并增加详情和编辑入口

## 4. 验证

- [x] 4.1 运行 `openspec validate add-archive-record-editing-layout --strict`
- [x] 4.2 运行后端 Maven 编译或测试
- [x] 4.3 运行 `vp check`

## 5. 高级筛选

- [x] 5.1 扩展 OpenSpec，定义范围条件和唯一字段可搜索合同
- [x] 5.2 扩展后端查询请求、条件转换和动态 SQL
- [x] 5.3 创建或修改唯一规则时自动将字段标记为可精确筛选并维护索引
- [x] 5.4 前端档案库增加高级筛选区，数字和日期类字段使用范围控件
- [x] 5.5 重新运行 OpenSpec、前端检查和后端测试

## 6. 公共布局修订

- [x] 6.1 清理个人布局合同：移除 `owner_user_id`、个人布局唯一索引和 `saveMyLayout` 等个人布局 API
- [x] 6.2 明确 `am_archive_field.list_width` 为字段基础列宽，表格布局默认读取该值
- [x] 6.3 布局表只保存公共布局的可见性、顺序、跨列和必要的列宽覆盖
- [x] 6.4 重新评估拖拽库，优先验证 `pragmatic-drag-and-drop` 与 Vue/Element Plus 的配合
