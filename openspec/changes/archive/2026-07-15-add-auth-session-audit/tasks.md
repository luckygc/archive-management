## 1. 后端审计模型

- [x] 1.1 新增 `am_authentication_event` Flyway 迁移，包含认证事件、用户、session、请求上下文和客户端摘要字段。
- [x] 1.2 新增认证审计实体、事件枚举和 Jakarta Data Repository。
- [x] 1.3 新增客户端上下文采集与轻量 User-Agent 摘要解析组件，并覆盖单元测试。

## 2. 后端登录与会话链路

- [x] 2.1 登录成功时保存 Spring Session 客户端属性并写入 `login_success` 审计日志。
- [x] 2.2 登录失败时写入 `login_failure` 审计日志。
- [x] 2.3 主动退出时写入 `logout` 审计日志。
- [x] 2.4 新增登录会话列表接口，从 Spring Session 读取在线会话并返回 cursor 分页响应，不使用 `AuthAuditMapper`。
- [x] 2.5 新增踢下线接口，写入 `kickout` 审计日志后删除目标 Spring Session，并禁止踢出当前会话。
- [x] 2.6 新增认证审计日志查询接口，支持事件类型、用户名、关键字和时间范围筛选。
- [x] 2.7 新增登录失败限制状态表、实体、Repository 和指数退避服务。
- [x] 2.8 登录流程在 CAP 消费前检查登录失败限制；失败后记录限制状态，成功后清除限制状态。
- [x] 2.9 新增短生命周期数据清理接口与 Quartz 作业，CAP 和登录失败限制接入，认证审计日志不清理。

## 3. 前端 API 与页面

- [x] 3.1 在 `frontend-core` 新增登录会话和认证审计类型与 API 方法。
- [x] 3.2 在 Web 端新增“登录会话”页面，展示在线 session 并支持踢下线。
- [x] 3.3 在 Web 端新增“认证审计”页面，展示认证事件日志并支持基础筛选。
- [x] 3.4 将两个页面加入系统配置菜单和路由。

## 4. 验证与文档

- [x] 4.1 补充登录成功、登录失败、主动退出、踢下线和列表查询测试。
- [x] 4.1.1 补充登录失败限制、CAP 不顺手清理和统一清理作业测试。
- [x] 4.2 运行后端定向 Maven 测试和前端定向检查/测试。
- [x] 4.3 更新登录认证规格相关 README 或说明文档。
