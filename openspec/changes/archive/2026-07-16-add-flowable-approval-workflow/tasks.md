## 1. Flowable 依赖和兼容验证

- [x] 1.1 在 `server/pom.xml` 增加 Flowable 版本属性和 `flowable-spring-boot-starter-process` 依赖
- [x] 1.2 增加最小 Flowable 配置，确认使用项目现有 PostgreSQL DataSource 和事务管理
- [x] 1.3 新增最小 BPMN 流程资源和启动测试，验证 Flowable 表初始化、部署、发起和用户任务查询
- [x] 1.4 运行 Maven 依赖解析和后端测试，确认 Spring Boot 4.1、Java 25、Hibernate 8 Beta 与 Flowable starter 兼容

## 2. 数据模型和迁移

- [x] 2.1 新增项目自有审批流定义、定义版本、实例、任务快照和审批意见表，表名使用 `am_approval_` 前缀
- [x] 2.2 明确 Flowable `ACT_*` 原生表为第三方框架表例外，不纳入项目自有表命名规则
- [x] 2.3 新增审批状态、任务状态、办理动作和候选人策略枚举或值对象
- [x] 2.4 为定义编码、业务绑定、待办查询和历史查询补齐 PostgreSQL 索引

## 3. Flowable 适配层

- [x] 3.1 新增 `infrastructure/flowable` 适配服务，封装部署 BPMN、启动实例、查询任务、完成任务和终止实例
- [x] 3.2 实现项目流程定义模型到 BPMN XML 的转换，首版支持开始事件、顺序用户任务和结束事件
- [x] 3.3 实现 Flowable 任务创建或查询结果到项目待办快照的同步逻辑
- [x] 3.4 增加 ArchUnit 规则，禁止非审批模块和非基础设施适配层直接依赖 `org.flowable`

## 4. 审批流业务模块

- [x] 4.1 新增审批流定义管理 Service 和 Controller
- [x] 4.2 实现审批流定义发布，保存项目版本与 Flowable deployment/process definition 映射
- [x] 4.3 实现发起审批流程实例，绑定业务类型、业务 ID、标题、发起人和定义版本
- [x] 4.4 实现我的待办、我发起的、已办和实例详情查询
- [x] 4.5 实现任务同意、驳回、撤回或终止动作，并保存审批意见和状态快照
- [x] 4.6 实现候选人策略解析接口，首版至少支持指定用户候选

## 5. API 和前端

- [x] 5.1 新增审批流 API 类型和请求封装，路径遵循 `/api/v1` 和 AIP 冒号动作
- [x] 5.2 新增审批流定义管理页面，支持草稿编辑、启停和发布
- [x] 5.3 新增审批中心页面，支持我的待办、我发起的、已办和详情查看
- [x] 5.4 新增审批办理视图，支持同意、驳回、意见填写和流程进度展示

## 6. 验证

- [x] 6.1 运行 `openspec validate add-flowable-approval-workflow --strict`
- [x] 6.2 运行后端架构测试和审批流相关测试
- [x] 6.3 运行前端 `vp check` 和 `vp test`
- [x] 6.4 补充最小人工验证说明，包括发布流程、发起实例、办理待办和查看历史

## 7. 交付清理

- [x] 7.1 删除“存储配置”和“系统参数”占位路由、占位组件及对应用户手册描述
- [x] 7.2 调整档案导入模板，仅生成表头，不附带示例数据行
- [x] 7.3 忽略仓库根目录运行时 `data/`，并移除已跟踪的运行时文件
- [x] 7.4 确认预览服务能力边界保持不变
